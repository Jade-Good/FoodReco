import { Link } from "react-router-dom";
import styled from "styled-components";

export const NoneStyledLink = styled(Link)`
  text-decoration: none;
  color: black;
  padding: 30px;
  /* 다른 원하는 스타일을 추가로 설정 가능 */
`;
